# Skrivnosti za backend. Skopiraj:  cp backend.env.example backend.env
# MONGODB_URI in ORV_API_URL sta ze nastavljena v compose (lokalni mongo + orv),
# zato ju tukaj ni treba vpisovati.
SESSION_SECRET=zamenjaj-z-dolgo-nakljucno-vrednostjo
