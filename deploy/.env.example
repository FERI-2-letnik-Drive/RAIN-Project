# Docker Hub uporabnisko ime, pod katerim so objavljene slike
# (orv-api, smartmailbox-backend, smartmailbox-frontend).
# Skopiraj v .env in vpisi svoje ime:  cp .env.example .env
DOCKERHUB_USERNAME=tvoj-dockerhub-uporabnik
